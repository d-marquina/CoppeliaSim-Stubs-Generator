"""
This is a runtime placeholder file for the `stubs.pyi` file.

Its sole purpose is to exist, allowing `import` statements to resolve successfully
at runtime. The actual type hints are defined in `stubs.pyi` and are used
by IDEs and static type checkers (like MyPy).

This file can be empty because type-only imports are handled within
`if TYPE_CHECKING:` blocks in the client code.
"""
pass
